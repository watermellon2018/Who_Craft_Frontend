import React from 'react';
import {Empty} from 'antd';

export default function ExpressionList() {
  return <Empty description="Expression assets will appear here after expression variant generation." />;
}

